        {% if score < 30 and score >= 0 %}
        <!-- Bronze Badge -->
        <div class="bg-gradient-to-r from-gray-700 via-brown-600 to-orange-700 shadow-2xl rounded-3xl text-center p-8 px-10 space-y-4 w-72 transform hover:scale-105 transition-transform duration-300">
            <div class="w-28 h-28 mx-auto mb-3">
                <div class="w-full h-full bg-gradient-to-br from-orange-700 to-yellow-700 shadow-xl rounded-full flex items-center justify-center">
                    <div class="w-20 h-20 bg-gradient-to-tr from-brown-300 to-orange-400 rounded-full shadow-inner"></div>
                </div>
            </div>
            <h3 class="text-3xl font-bold text-orange-600 tracking-wider">Bronze</h3>
            <p id="score" class="opacity-90 rounded-lg bg-orange-100 text-orange-800 font-extrabold text-8xl p-3 shadow-md">{{ score }}</p>
        </div>
        {% endif %}

        {% if score < 60 and score >= 30 %}
        <!-- Silver Badge -->
        <div class="bg-gradient-to-r from-gray-800 to-gray-700 shadow-2xl rounded-3xl text-center p-8 px-10 space-y-4 w-72 transform hover:scale-105 transition-transform duration-300">
            <div class="w-28 h-28 mx-auto mb-3">
                <div class="w-full h-full bg-gradient-to-br from-gray-400 to-gray-600 shadow-xl rounded-full flex items-center justify-center">
                    <div class="w-20 h-20 bg-gradient-to-tr from-white to-gray-300 rounded-full shadow-inner"></div>
                </div>
            </div>
            <h3 class="text-3xl font-bold text-white tracking-wider">Argent</h3>
            <p id="score" class="opacity-90 rounded-lg bg-gray-100 text-gray-800 font-extrabold text-8xl p-3 shadow-md">{{ score }}</p>
        </div>
        {% endif %}
        {% if score >= 60 %}
        <!-- Gold Badge -->
        <div class="bg-gradient-to-r from-yellow-500 via-amber-500 to-yellow-400 shadow-2xl rounded-3xl text-center p-8 px-10 space-y-4 w-72 transform hover:scale-105 transition-transform duration-300">
            <div class="w-28 h-28 mx-auto mb-3">
                <div class="w-full h-full bg-gradient-to-br from-yellow-400 to-amber-600 shadow-xl rounded-full flex items-center justify-center">
                    <div class="w-20 h-20 bg-gradient-to-tr from-yellow-200 to-yellow-400 rounded-full shadow-inner"></div>
                </div>
            </div>
            <h3 class="text-3xl font-bold text-yellow-400 tracking-wider">Or</h3>
            <p id="score" class="opacity-90 rounded-lg bg-yellow-100 text-yellow-800 font-extrabold text-8xl p-3 shadow-md">{{ score }}</p>
        </div>
        {% endif %}
        <!-- Silver Badge -->
        {# <div class="bg-white shadow-lg rounded-3xl text-center p-6 px-8 space-y-2">
            <div class="w-24 h-24 mx-auto mb-2">
                <div class="w-full h-full bg-gradient-to-r from-gray-500 to-gray-400 shadow-lg rounded-full"></div>
            </div>
            <h3 class="text-2xl font-bold text-gray-600">SILVER</h3>
            <p class="bg-gray-300 opacity-50 rounded-lg text-black font-semibold text-2xl p-1">55</p>
            <h3 class="text-2xl font-bold text-gray-600">COURS</h3>
        </div> #}

        <!-- Bronze Badge -->
        {# <div class="bg-white shadow-lg rounded-3xl text-center p-6 px-8 space-y-2">
            <div class="w-24 h-24 mx-auto mb-2">
                <div class="w-full h-full bg-gradient-to-r from-[#d4885d] to-orange-800 shadow-lg rounded-full"></div>
            </div>
            <h3 class="text-2xl font-bold text-orange-600">BRONZE</h3>
            <p class="bg-[#d4885d] opacity-50 rounded-lg text-black font-semibold text-2xl p-1">55</p>
            <h3 class="text-2xl font-bold text-orange-600">CHALLENGE</h3>
        </div> #}